const Resume = () => {
  //const req = await fetch(`http://localhost:3333/api/resume/${params.resumeId}`);

  return (
    <div>
      <h1 className="text-4xl font-bold mb-4">Here's your Resume</h1>
    </div>
  );
};

export default Resume;
